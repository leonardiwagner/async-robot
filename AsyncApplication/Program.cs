using System;

namespace AsyncApplication
{
    class Program
    {
        static void Main(string[] args)
        {
            Land land = new Land();
            /*
            Test test = new Test();

            //Synchronous threads
            test.DoTest(0);
            test.DoTest(1);
            test.DoTest(2);
            test.DoTest(3);
            test.DoTest(4);

            //Asynchronous threads
            test.DoTestAsync(0);
            test.DoTestAsync(1);
            test.DoTestAsync(2);
            test.DoTestAsync(3);
            test.DoTestAsync(4);

            Console.Read();*/
        }
    }

    
}
